# Configuration

What it does: Holds the settings for the AI model.

## In simple terms

This folder tells the system which "brain" to use when reviewing code.

The config file says: "Use Amazon Nova Lite" - a fast, affordable AI that's good at code review.

## Why it matters

Everything in the system uses the same AI model. If you want to try a different AI later, you change this one setting.