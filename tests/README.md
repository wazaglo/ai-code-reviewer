# Tests

What it does: Makes sure everything works correctly.

## In simple terms

These are automated checks that run before deployment:
- Did I break the GitHub provider? Tests say yes/no
- Does the worker still post comments correctly? Tests say yes/no
- Are the secrets being fetched right? Tests say yes/no

## Why it matters

Tests are your safety net. They catch bugs before they reach production.